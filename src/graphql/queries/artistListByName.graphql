query artistListByName($filter: ArtistSearchFilter!) {
  artistListByName(filter: $filter) {
    pages {
      start
      limit
      total
    }
    artists {
      id
      name
      genres
      image
      href
      spotifyUri
    }
  }
}
